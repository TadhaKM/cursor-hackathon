# Frontend (Vite) — base URLs for the three backend services.
# Copy to .env.local for local dev. If ANY of these is unset, the frontend
# falls back to its built-in mock pipeline (see src/api.ts).
#
# Local dev ports (each service's default):
#   repo-ingest    -> 3000   (POST /ingest)
#   repo-explainer -> 8787   (POST /explain, POST /chat)
#   video-renderer -> 8000   (POST /render, GET /render/:job_id)

VITE_INGEST_URL=http://localhost:3000
VITE_EXPLAIN_URL=http://localhost:8787
VITE_RENDER_URL=http://localhost:8000

# Chat (both the result-page chat and the floating "?" assistant) reuses
# VITE_EXPLAIN_URL above — it calls repo-explainer's own /chat endpoint
# (Qwen + RAG, already keyed server-side via QWEN_API_KEY in that service).
# No separate chat URL or API key needed here.
